<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:AlgorithmicsApp.Models"
             x:Class="AlgorithmicsApp.Views.TheoryPage"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:local="clr-namespace:AlgorithmicsApp.ViewModels"
             xmlns:math="clr-namespace:CSharpMath.Forms;assembly=CSharpMath.Forms"
             Title="{Binding TheoryTitle}">


    <RefreshView x:DataType="local:TheoryViewModel" Command="{Binding LoadTheoryContentCommand}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
        <ListView
            BackgroundColor="Transparent"
            CachingStrategy="RecycleElement"
            HasUnevenRows="True"
            ItemsSource="{Binding TheoryContentList}"
            SelectionMode="None"
            SeparatorVisibility="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:TheoryContent">
                    <ViewCell>
                        <StackLayout VerticalOptions="Center">
                            <Label
                                FontSize="Large"
                                Text="{Binding Text}" 
                                TextType="Html"
                                LineBreakMode="WordWrap"
                                HorizontalTextAlignment="Start"
                            />
                            <math:MathView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" LaTeX="{Binding Formula}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </RefreshView>
</ContentPage>